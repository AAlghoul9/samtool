streamlit==1.49.1
transformers==4.56.1
Pillow>=10.0.0
numpy<2.0
matplotlib>=3.8

# DINOv2 is loaded via torch.hub, no pip package needed
# Note: Install torch/torchvision separately to match your platform (handled by setup script)

